#include<stdio.h>
#include<math.h>
int main()
{
    // int ans=ceil(4.3);
    //  int ans=floor(3.9);
    //  int ans=round(4.4); // up to 0.5
    // double ans=sqrt(20);
    // printf("%lf",ans);
    // int ans=pow(4,3); // 4 holo n; 3 holo power
    int ans=abs(-12); // negative ignore kore & Positive thakle positive e thake
    printf("%d",ans);
    return 0;
}